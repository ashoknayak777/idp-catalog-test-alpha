apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: catalog-test-alpha
  description: Test service alpha for IDP Catalog Info YAML integration
  tags:
    - test
    - backend
spec:
  type: service
  lifecycle: experimental
  owner: user:guest
